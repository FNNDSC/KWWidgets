PROJECT(TkDnD)

SET(TkDnD_SRCS)

IF(WIN32)
  INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/win)
  SET(TkDnD_SRCS ${TkDnD_SRCS}
    ${CMAKE_CURRENT_SOURCE_DIR}/win/TkDND_OleDND.cpp)
ENDIF(WIN32)

IF(UNIX)
  SET(TkDnD_SRCS ${TkDnD_SRCS}
    ${CMAKE_CURRENT_SOURCE_DIR}/unix/TkDND_XDND.c)
ENDIF(UNIX)

IF(TkDnD_SRCS)
  ADD_LIBRARY(kwtkdnd STATIC ${TkDnD_SRCS})
  TARGET_LINK_LIBRARIES(kwtkdnd ${TCL_LIBRARY} ${TK_LIBRARY})
ENDIF(TkDnD_SRCS)
